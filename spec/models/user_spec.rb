require 'spec_helper'
#
# TODO: revise rspec according to the Devise implementation
#
describe User do
  # before do
  # 	@user = User.new(email: "example@example.com", password: "20characterspassword", password_confirmation: "20characterspassword")
  # end

  # subject { @user }

  # it { should respond_to :email }
  # it { should respond_to :password }
  # it { should respond_to :password_confirmation }
  # it { should be_valid }

  # describe "when email is nil" do
  # 	before { @user.email = nil }
  # 	it { should_not be_valid }
  # end

  # describe "when password is not present" do
  # 	before { @user.password = nil }
  # 	it { should_not be_valid }
  # end

  # describe "when password shorter than 8 characters" do
  # 	before { @user.password = 'a' * 7 }
  # 	it { should_not be_valid }
  # end

  # describe "when password does not match confirmation" do
  # 	before { @user.password_confirmation = @user.password + 'x' }
  # 	it { should_not be_valid }
  # end
end
